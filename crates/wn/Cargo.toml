[package]
name = "wn"
version = "0.1.0"
edition = "2024"

[lib]
name = "wn"

[dependencies]
miette  = { version = "7", features = ["fancy"] }
thiserror = "2"
id-arena = "2"

[package.metadata.dist]
dist = false

[dev-dependencies]
insta = { version = "1", features = ["ron"] }
